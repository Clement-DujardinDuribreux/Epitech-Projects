/*
** EPITECH PROJECT, 2026
** Ressources.hpp
** File description:
** Ressources enum definition
*/

#ifndef RESSOURCES
    #define RESSOURCES

/**
 * @enum Ressources
 * @brief Resource identifiers used by inventories, tiles, and the protocol.
 */
enum Ressources {
    /** Food consumed by players to stay alive. */
    Food,
    /** First elevation stone. */
    Linemate,
    /** Second elevation stone. */
    Deraumere,
    /** Third elevation stone. */
    Sibur,
    /** Fourth elevation stone. */
    Mendiane,
    /** Fifth elevation stone. */
    Phiras,
    /** Final elevation stone. */
    Thystame
};

#endif
