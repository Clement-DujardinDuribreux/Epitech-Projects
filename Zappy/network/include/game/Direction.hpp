/*
** EPITECH PROJECT, 2026
** Ressources.hpp
** File description:
** Ressources enum definition
*/

#ifndef DIRECTIONS
    #define DIRECTIONS

/**
 * @enum Directions
 * @brief Cardinal orientations used by players and protocol events.
 */
enum Directions {
    /** Player faces the top side of the map. */
    North,
    /** Player faces the bottom side of the map. */
    South,
    /** Player faces the right side of the map. */
    East,
    /** Player faces the left side of the map. */
    West
};

#endif
